Next steps:

- make it auto rename
- see how moving files is set up
- compare guids for missing meshes

consider: packages section of db as, well, indexed db? and non-indexed go elsewhere? dunno

save file size. if size changes, rescan? save location; if it goes away, remove